/**
 * @file const.ts
 * @description ثوابت مشتركة تستخدم عبر العميل والخادم
 * يوفر ثوابت شائعة لإدارة الجلسة وحسابات الوقت
 * يستخدمه الواجهة الخلفية والواجهة الأمامية للاتساق
 */

/**
 * @constant COOKIE_NAME
 * @description اسم ملف تعريف الجلسة المستخدم للمصادقة
 * يخزن معرف جلسة المستخدم عبر الطلبات
 */
export const COOKIE_NAME = "app_session_id";

/**
 * @constant ONE_YEAR_MS
 * @description سنة واحدة بالمللي ثانية
 * يستخدم لضبط أوقات انتهاء صلاحية ملفات تعريف الارتباط طويلة الأمد
 * محسوب كـ: 1000مللي ثانية * 60ثانية * 60دقيقة * 24ساعة * 365يوم
 */
export const ONE_YEAR_MS = 1000 * 60 * 60 * 24 * 365;
